import { createStore } from 'vuex'
import settings from './modules/settings.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    settings
  }
})
